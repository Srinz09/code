/** Photo/video slots ready for real client stories — no testimonial is invented. */
export const testimonialSlotIds: string[] = [1, 2, 3].map((i) => `testimonial-photo-${i}`);
export const videoSlotIds: string[] = [1, 2, 3, 4].map((i) => `video-thumb-${i}`);
